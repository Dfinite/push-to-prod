fastapi>=0.110,<0.116
uvicorn[standard]>=0.27,<0.31
# 1.2.x 로 핀: 구버전(0.2.x)은 노드명이 state 키와 겹치면 거부(review_questions 등)해
# 그래프 빌드가 깨진다. A2 가 검증한 버전(1.2.5)에 맞춘다.
langgraph>=1.2,<2
pydantic>=2.7,<3
anthropic>=0.30,<2.0
python-dotenv>=1.0,<2
# A2 retrieve 노드: 레퍼런스 Postgres introspection (merge-test 통합 시 추가). duckdb는 미사용.
psycopg[binary]>=3.1,<4
